module.exports = {
  db: require('./db.config'),
  payment: require('./payment.config'),
  tokens: require('./tokens.config'),
  fetchers: require('./fetchers.config'),
  logger: require('./logger.config'),
  service: require('./service.config'),
  rates: require('./rates.config')
};
